<!--index.wxml-->

<import src="../../tpl/officialMsgCard.wxml" />

<view class="container">
    <!--
	<view class='' wx:if='{{icoList.length === 0}}'>
		<image src="../../pic/undefined.png" />
	</view>
    -->

	<view class='official-list'>
        <template is="officialMsgCard" wx:for='{{officialContentList}}' data='{{item}}'></template>
        <!--
		<template is="msgCard" data='{{icoList}}'></template>
		<view class='ico-last' wx:if='{{isFinish}}'>
			已加载完！
		</view>
		<view class='ico-last' wx:if='{{isRequestGetIcoList}}'>
			正在加载中...
		</view>
        -->
	</view>

</view>